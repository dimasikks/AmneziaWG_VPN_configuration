#!/bin/bash

MAIN="/etc/amnezia/amneziawg"
USER_ID=$1

(
    flock 200
    sed -i "/# USER - ${USER_ID}/,+4d" "$MAIN/server.conf"
) 200>"$MAIN/server.conf.lock"

awg syncconf server <(awg-quick strip server)